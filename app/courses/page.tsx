import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function CoursesPage() {
  return (
    <PagePlaceholder
      emoji="🛒"
      title="Courses magiques"
      tagline="Fusion automatique des ingrédients, classement par rayon, items cochables. Le chaos part faire un tour."
      hint="Liste fusionnée — étape suivante"
    />
  );
}
