import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function GardeMangerPage() {
  return (
    <PagePlaceholder
      emoji="🧂"
      title="Le garde-manger"
      tagline="Ce qu'on a déjà sous la main. Bientôt, on saura ce qu'on peut cuisiner sans aller au supermarché."
      hint="Inventaire + filtre — phase 3"
    />
  );
}
