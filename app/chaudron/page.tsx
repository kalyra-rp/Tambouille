import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function ChaudronPage() {
  return (
    <PagePlaceholder
      emoji="🍲"
      title="Le Chaudron"
      tagline="Les recettes sélectionnées pour la magie. Ajuste les portions, laisse fusionner les ingrédients, savoure le chaos vaincu."
      hint="Sélection + portions — étape suivante"
    />
  );
}
