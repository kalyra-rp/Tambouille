import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function ChouchousPage() {
  return (
    <PagePlaceholder
      emoji="❤️"
      title="Les chouchous de la famille"
      tagline="Tes recettes préférées, celles qu'on redemande à chaque repas. Le best-of, sans mauvaise surprise."
      hint="Favoris + notes — étape suivante"
    />
  );
}
