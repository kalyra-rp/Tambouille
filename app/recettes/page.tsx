import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function RecettesPage() {
  return (
    <PagePlaceholder
      emoji="📚"
      title="Ta bibliothèque gourmande"
      tagline="Toutes tes recettes, filtrables par catégorie, tag et statut. À tester, validées famille, chouchous — chacun trouve sa case."
      hint="Grille + filtres — étape suivante"
    />
  );
}
